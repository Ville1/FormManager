namespace FormManager.Data
{
    public enum LogEventType { Login }

    public enum FilterType { Text }

    public enum VideoGameGenre { Action, Adventure, Fighting, Platform, Puzzle, Racing, RolePlaying, Shooter, Simulation, Sports, Strategy, Misc }
}
